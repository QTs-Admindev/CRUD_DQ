node_modules/
dist/
.serverless/
.esbuild/

# Local env files — never commit these
.env
.env.local

# Staged env files are safe to commit (no secrets, only URLs)
# .env.dev  ← committed intentionally as reference
# .env.prod ← committed intentionally as reference
